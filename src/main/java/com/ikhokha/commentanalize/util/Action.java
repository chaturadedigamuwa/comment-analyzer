package com.ikhokha.commentanalize.util;

/**
 * this enum contains types of string operations to use in the factory provider.
 * When new operation type comes then new enum should be added
 */
public enum Action {

    LENGTH,
    WORD

}
